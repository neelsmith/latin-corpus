package edu.holycross.shot.latincorpus

import edu.holycross.shot.tabulae._

import org.scalatest.FlatSpec

class SequenceCollectionClusteringSpec extends FlatSpec {

  "A ParsedSequenceCollection" should "recluster all tokens as LatinEditions" in pending
  it should "recluster all tokens as citableUnits"  in pending
  it should "recluster all tokens as ngrams" in pending
}
